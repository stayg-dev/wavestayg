# 2026-09-11 Figma 반영

원본: `7GQ2ihw8K70UklPTEQfD1X`, 페이지 `0:1`.

- 홈 `1:3`: 소개 본문, 객실 3종, 부대시설 3종과 탭, 주변 명소 카드 5개.
- 객실 `50:145`: 5개 분류 필터, 11개 객실 목록, 스크롤 목록과 선택 상세.
- 별도 객실 `138:933`, `138:1085`, `138:1135`, `138:1185`, `138:1285`, `138:1235`, `138:1335`, `138:1385`, `138:1435`, `138:1636`, `138:1686`: 객실명, 설명, 배지, 제원, 가격.
- 부대시설 `50:924`, 홈 컴포넌트 `16:257`, `16:259`, `16:287`: 루프탑, 프런트 데스크, 주차장.
- 오시는 길 `50:1232`: 전화·이메일 변경. 동일 연락처를 문의·예약 안내·푸터에도 사용.
- 예약 선택 `60:2606`: 11개 타입과 디자인의 예약 가능 상태. 선택 타입을 요약·확인·조회에도 연결.

## 합의된 기준

사용자가 **11개 타입 + 별도 컴포넌트 기준**을 선택했다. 홈의 7타입 표기는 실제 데이터 수인 11로 변경했다. 홈·목록·예약 화면의 서로 다른 요금은 별도 컴포넌트의 58,000원으로 통일했다. 예약 가능 상태는 기존과 같은 디자인 미리보기이며 실제 재고가 아니다.

객실 데이터는 `src/lib/rooms.ts`, 표시 컴포넌트는 `src/components/rooms.tsx`로 분리했다. 다른 화면에서 해당 데이터를 재사용한다.

## 원본에 남은 내용 불일치

별도 컴포넌트 내부의 배지와 상세 제원도 일부 다르다. 임의로 실제 정보를 추정하지 않고 각각 원문을 반영했다.

- 주니어 더블: 별도 컴포넌트의 배지 39㎡, 상세 23㎡, 목록은 32㎡로 서로 다르다.
- 로프트: 상세 면적에는 복층 표기가 없고 배지에만 복층이 적혀 있다.
- 디럭스 트윈: 목록 설명은 싱글 두 개로 바뀌었지만 별도 컴포넌트 설명에는 더블과 싱글이 함께 있다고 적혀 있다. 합의에 따라 별도 컴포넌트를 우선했다.
- 부대시설 배너는 네 개의 공간이라고 적혀 있으나 실제 프레임은 세 개다.

사진이 없는 회색 영역은 원본대로 유지한다. 다음 원본 수정 시 각 객실의 `nodeId`로 대응할 수 있다.

## 재대조 반영

최신 원본을 다시 불러와 홈·객실·11개 상세·부대시설·오시는 길을 대조했다.

- 추가 수정된 원본 문구: 홈 객실 제목, 객실 배너, 인원 라벨, 디럭스 더블 침대 설명.
- 추가 수정된 원본 제원: 주니어·프리미엄 최대 3인, 로프트 최대 4인, 해당 타입 오션뷰, 프리미엄 39㎡, 로프트 패밀리 32㎡.
- 앞선 구현에서 놓친 배치: 부대시설은 프런트 데스크 → 루프탑 → 주차장, 교통 안내는 배경 패널 안의 4열 카드, 주변 명소는 사진 없는 5열 카드.
- 목록에서 주니어 패밀리를 주니어 더블 앞에 배치하되 예약의 기존 객실 ID는 유지한다.
- 객실 상세 464px 기준 여백·행간·제원 2열·예약 버튼 배치와 설명 줄바꿈, 홈 소개 영역의 폭을 보정했다.

브라우저 연결이 없어 로컬 화면의 시각적 일치와 클릭 동작은 검증하지 못했다. 원본 이미지·디자인 속성과 코드 비교, 린트·빌드·정적 HTML 확인으로 검증했다.
